# crispr_library_pool_analysis
Pipeline to generate counts

Upon cloning repository, please create a folder called "data_files" to store fastq files and create a folder called "library_refs" to store reference files.

Packages required:
biopython
cutadapt
matplotlib
FLASH
